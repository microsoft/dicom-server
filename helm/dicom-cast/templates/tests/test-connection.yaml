apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dicom-cast.fullname" . }}-test-connection"
  labels:
    {{- include "dicom-cast.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dicom-cast.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
