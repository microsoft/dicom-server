apiVersion: v1
kind: Service
metadata:
  name: {{ include "dicom-cast.fullname" . }}
  labels:
    {{- include "dicom-cast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dicom-cast.selectorLabels" . | nindent 4 }}
