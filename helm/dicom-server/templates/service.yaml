apiVersion: v1
kind: Service
metadata:
  name: {{ include "dicom-server.fullname" . }}
  labels:
    {{- include "dicom-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dicom-server.selectorLabels" . | nindent 4 }}
