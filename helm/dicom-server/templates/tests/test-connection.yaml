apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dicom-server.fullname" . }}-test-connection"
  labels:
    {{- include "dicom-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dicom-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
